---
label: Directory page
hide_body: true
fields:
- name: hero
  type: include
  config: {}
  template: hero
  label: Hero
- name: subpage_cards
  type: field_group_list
  fields:
  - name: title
    type: text
    config:
      required: false
    label: Page Title
  - name: url
    type: text
    config:
      required: false
    label: Page URL
    description: If the URL is located within the site, then <strong>begin the text
      with "/"</strong> (i.e. "/about-us"). Otherwise,<strong> begin the text with
      "https://"</strong> (i.e. "https://www.google.com").
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
    description: 'Recommended Image Dimensions: 545x665'
  config:
    min: 
    max: 
    labelField: 
  label: Subpage Cards
  description: Creates cards that link to subpages.
- name: seo
  type: include
  config: {}
  template: seo
  label: SEO
pages:
- content/data/pages/Connect.json
