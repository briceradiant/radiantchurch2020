---
label: Home Feature
hide_body: true
fields:
- name: home_feature_headline
  type: textarea
  default: ''
  config:
    required: true
    wysiwyg: true
    schema:
      format: html
  label: Headline
  description: Italicize the word/phrase you'd like to add emphasis to. This will
    add a brush stroked underline or circle around that word/phrase.
- name: home_feature_bodytext
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: true
    schema:
      format: html
  label: Bodytext
- name: home_feature_image
  type: file
  config:
    maxSize: 5
  label: Image
  description: 'Recommended Image Dimensions: 1050x1350'
- name: home_feature_cta_text
  type: text
  config:
    required: false
  label: CTA Button Text
- name: home_feature_cta_url
  type: text
  config:
    required: false
  label: CTA Button URL
  description: If the URL is located within the site, then <strong>begin the text
    with "/"</strong> (i.e. "/about-us"). Otherwise,<strong> begin the text with "https://"</strong>
    (i.e. "https://www.google.com").
